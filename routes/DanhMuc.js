const express = require('express');
const router = express.Router();

//

router.get('/:name/danh-sach-san-pham', async function (req, res) {
    const name = req.params.name
    
})

module.exports = router;